spring:
  datasource:
#    url: jdbc:postgresql://172.17.0.2:5432/management
    url: jdbc:postgresql://localhost:5432/management
    username: ankhangnumber1
    password: ankhangnumber1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080